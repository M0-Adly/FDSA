/* ─── Supabase Static Hosting Configuration ─────────────────────────────── */
/*
   This SPA is hosted via Supabase Storage (public bucket "site").
   Vite builds to /dist — upload that folder to the bucket root.

   If using Vercel (recommended shortcut):
     vercel --prod
   connects to the same GitHub repo and auto-deploys.
*/
